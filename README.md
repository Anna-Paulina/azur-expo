# Azur — build EAS

```
npm install
npx eas-cli login
npx eas-cli build -p android --profile preview
```

Le lien de téléchargement de l'APK s'affiche à la fin du build (et reste visible sur expo.dev).
